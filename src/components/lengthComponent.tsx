// import { useRecoilValue } from "recoil";
// import { lengthState } from "../../recoil/selectors/name";

export default function LengthComponent() {
  // const length = useRecoilValue(lengthState);
  return <h2>Bofje</h2>;
}
